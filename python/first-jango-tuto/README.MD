# FIRST DJANGO APP TUTORIAL

Is necessary follow venv with 'requirements.txt',
the venv can be on preferenced dir, but
requirements will be inside 'mysite' from tutorial

commands:
python -m venv for-first-jango
source for-first-jango/bin/activate
python -m pip install --upgrade pip
pip install -r requirements.txt
